import styles from "./input.module.css";
import { createStyledComponent } from "../_internal/create-styled-component";

export const Input = createStyledComponent(
  "input",
  styles["sw-input-root-2fm124"],
);
