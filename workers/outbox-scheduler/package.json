{
  "name": "@shapewebs/outbox-scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --env staging --dry-run --outdir .wrangler/build",
    "test:runtime": "vitest run --config vitest.config.ts",
    "typecheck": "wrangler types --env staging --check && tsc --project tsconfig.json --noEmit",
    "types": "wrangler types --env staging"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.8",
    "typescript": "^5",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  }
}
